import Gui from "./Gui";
import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
    <React.StrictMode>
        <Gui />
    </React.StrictMode>,
    document.getElementById("root")
);
