import React from "react";
import ReactDOM from "react-dom";
import Gui from "./Gui";

ReactDOM.render(
    <React.StrictMode>
        <Gui />
    </React.StrictMode>,
    document.getElementById("root")
);
